/**
 * @param {number} n
 * @return {number[][]}
 */

var generateMatrix = function(n) {
  var dir = [
    right: {x: 1, y: 0},
    down: {x: 0, y: 1},
    left: {x: -1, y: 0},
    up: {x: 0, y: -1}
  ]

  var ret = []
  var i, j

  for (i = 0; i < n; i++) {
    ret[i] = []
    for (j = 0; j < n; j++) {
      ret[i][j] = new
    }
  }

  i = 0
  j = 0
  while (true) {
    if ()
  }

  return ret
};

console.log(generateMatrix(3))
